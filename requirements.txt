# Dependencies for the AppDaemon container and local dev.
# AppDaemon itself + Python packages used by apps.
# This is the single source of truth for pip install.

appdaemon>=4.2
croniter==1.3.8
requests>=2.31
